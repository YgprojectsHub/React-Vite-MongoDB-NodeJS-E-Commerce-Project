<!DOCTYPE html>
<html lang="tr">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <style>
      body {
        font-family: "Segoe UI", Tahoma, Geneva, Verdana, sans-serif;
        background-color: #f0f2f5;
        color: #333;
        padding: 20px;
        display: flex;
        align-items: center;
        justify-content: center;
        height: 100vh;
      }
      .container {
        background-color: #ffffff;
        padding: 40px;
        border-radius: 12px;
        max-width: 450px;
        box-shadow: 0 8px 20px rgba(0, 0, 0, 0.1);
        text-align: center;
      }
      h1 {
        color: #007bff;
        font-size: 24px;
        margin-bottom: 20px;
      }
      .message {
        font-size: 16px;
        line-height: 1.6;
        color: #555;
        margin-bottom: 30px;
      }
      .code {
        font-size: 26px;
        font-weight: bold;
        background-color: #f9f9f9;
        padding: 15px 30px;
        display: inline-block;
        letter-spacing: 4px;
        border-radius: 8px;
        color: #28a745;
        border: 2px solid #28a745;
        margin-bottom: 30px;
      }
      .footer {
        font-size: 14px;
        color: #999;
        margin-top: 30px;
      }
    </style>
  </head>
  <body>
    <div class="container">
      <% if (isEmailVer) { %>
      <p class="message">
        Sayın <%= name %>, email adresinizi doğrulamak için aşağıdaki kodu
        kullanın. Lütfen bu kodu kimseyle paylaşmayın.
      </p>
      <% } else { %>
      <p class="message">
        Sayın <%= name %>, işleminizi doğrulamak için aşağıdaki kodu kullanın.
        Lütfen bu kodu kimseyle paylaşmayın.
      </p>
      <% } %>
      <div class="code"><%= verificationCode %></div>
      <div class="footer">
        Eğer bu işlemi siz talep etmediyseniz, lütfen bu e-postayı dikkate
        almayın.
      </div>
    </div>
  </body>
</html>
